import 'package:flutter/material.dart';

class CompanyColors {
  // Colors extracted from your BharatNXT logo:
  // Deep blue for text and UI elements (e.g. "India ka Payment")
  static const Color deepBlue = Color(0xFF0F4C81);

  // Golden yellow for highlights (e.g. "BharatNXT")
  static const Color goldenYellow = Color(0xFFFFC107);

  // For our theme we'll use:
  static const Color primary = deepBlue;       // Primary color (deep blue)
  static const Color secondary = goldenYellow;   // Secondary/accent color (golden yellow)
  static const Color background = Colors.white;  // Background is white
}